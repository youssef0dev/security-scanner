{
  "name": "security-scanner-app",
  "version": "1.0.0",
  "description": "Security Scanner Web Application",
  "private": true,
  "scripts": {
    "build": "pip install -r requirements.txt && gunicorn app:app"
  },
  "dependencies": {},
  "devDependencies": {}
} 