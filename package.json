{
  "name": "security-scanner-app",
  "version": "1.0.0",
  "description": "Security Scanner Web Application",
  "scripts": {
    "build": "pip install -r requirements.txt"
  },
  "devDependencies": {
    "@netlify/plugin-python-core": "^1.0.0"
  }
} 